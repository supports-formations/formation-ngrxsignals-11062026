import { Routes } from "@angular/router";
import { DisplayDelivers } from "./components/display-delivers/display-delivers";

export const deliversRoutes: Routes = [
    {
        path: '',
        component: DisplayDelivers
    }
]